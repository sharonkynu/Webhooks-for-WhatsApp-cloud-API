<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WhatsApp Webhook</title>
    <style>
        /* Basic reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Helvetica Neue', sans-serif;
            background: linear-gradient(to right, #6a11cb, #2575fc); /* Gradient of purple and blue */
            color: #fff;
            padding: 20px;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        h1 {
            font-size: 3em;
            margin-bottom: 20px;
            color: #fff;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.4); /* Soft glow effect */
        }

        .container {
            max-width: 800px;
            width: 100%;
            background-color: #ffffff;
            border-radius: 15px;
            padding: 40px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
            transition: all 0.4s ease;
            text-align: center;
        }

        .container:hover {
            transform: scale(1.05);
        }

        .status {
            background-color: #f0f4f8;
            border: 2px solid #3a8cfc;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
            color: #333;
            transition: background-color 0.3s ease, border-color 0.3s ease;
        }

        .status:hover {
            background-color: #d9e7f5; /* Light blue hover */
            border-color: #1d74f5;
        }

        .status p {
            font-size: 1.4em;
            color: #333;
            margin: 0;
        }

        /* Button style */
        .button {
            display: inline-block;
            background-color: #1d74f5; /* Bright blue */
            color: #fff;
            padding: 15px 30px;
            margin-top: 30px;
            border-radius: 50px;
            text-decoration: none;
            font-size: 1.2em;
            text-align: center;
            transition: all 0.3s ease;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
        }

        .button:hover {
            background-color: #155fbd;
            transform: scale(1.05);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        }

        /* Footer styling */
        .footer {
            text-align: center;
            margin-top: 50px;
            color: #fff;
            font-size: 1em;
        }

        .footer a {
            color: #fff;
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s ease;
        }

        .footer a:hover {
            color: #ffbf00; /* Golden color on hover */
            text-decoration: underline;
        }

        /* Animation for smooth fade-in */
        @keyframes fadeIn {
            0% { opacity: 0; }
            100% { opacity: 1; }
        }

        body {
            animation: fadeIn 1.5s ease-in-out;
        }

    </style>
</head>
<body>
    <h1>Welcome to the WhatsApp Webhook!</h1>
    <div class="container">
        <div class="status">
            <% if (statusMessage) { %>
                <p><strong>Status:</strong> <%= statusMessage %></p>
            <% } else { %>
                <p><strong>Status:</strong> Webhook is awaiting messages.</p>
            <% } %>
        </div>

        <!-- Button to refresh status -->
        <a href="/" class="button">Refresh Status</a>
    </div>

    <div class="footer">
        <p>Created with ❤️ by <a href="https://github.com" target="_blank">kynu</a></p>
    </div>
</body>
</html>

